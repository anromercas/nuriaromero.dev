# SEO audit backlog 2026-09-22

## Objective
Execute the 12 actionable tasks from the second SEO audit round (`docs/seo-audit/full-audit-2026-09-22/tasks/`, SEO-15 to SEO-26), one at a time per user instruction, with one work-unit commit per task on `develop` and review before push.

## Problem
The 2026-09-22 live-site audit (11 parallel specialists + synthesis) confirmed SEO-01/03/09/11/12 resolved in production, but surfaced 12 new/still-open issues: consent-dialog accessibility (3 convergent sources), WhatsApp button overlap at 360px, templated niche-page FAQ closing text (same as 2026-09-21, unresolved), missing "agencia" in /seo-local-sevilla/ title/H1, trailing-slash gaps in shared Header/Footer, FAQ duplication across niche/service pages, and others. Full detail in `docs/seo-audit/full-audit-2026-09-22/FULL-AUDIT-REPORT.md` and `ACTION-PLAN.md`.

## Why
User explicitly authorized executing the backlog "poco a poco" (one task at a time), each followed by its own commit and review, rather than batching.

## Constraints
- Work on `develop`; one task at a time, user reviews after each.
- Preserve unrelated untracked files.
- Never invent GBP, testimonial, ranking, traffic, or backlink evidence.
- Conventional Commits, no AI attribution (per user's global CLAUDE.md rule).

## Effective TDD
- Mode: enabled (Strict TDD Mode, per session config).
- Source: session instructions.
- Runner: project scripts (`npm run build`; existing `check:seo-*` scripts; `npm test`).

## Checklist
- [x] SEO-15 Trailing slash in Header/Footer — `docs/seo-audit/full-audit-2026-09-22/tasks/15-trailing-slash-header-footer.md`
- [x] SEO-16 Consent panel ARIA role, button size, overflow, Tab reachability — `docs/seo-audit/full-audit-2026-09-22/tasks/16-accesibilidad-panel-consentimiento.md`
- [x] SEO-17 WhatsApp floating button overlap with hero CTA at 360px — `docs/seo-audit/full-audit-2026-09-22/tasks/17-solape-whatsapp-flotante.md`
- [x] SEO-18 Rewrite templated niche-page FAQ closing — `docs/seo-audit/full-audit-2026-09-22/tasks/18-reescribir-faq-cierre-nicho.md`
- [x] SEO-19 Differentiate near-duplicate price FAQ/H2 (comercios/tienda-online, diseño-web/blog) + extend check-seo-07 — `docs/seo-audit/full-audit-2026-09-22/tasks/19-diferenciar-faq-precio-duplicada.md`
- [x] SEO-20 Split long FAQ answers into scannable blocks — `docs/seo-audit/full-audit-2026-09-22/tasks/20-dividir-faq-largas.md`
- [x] SEO-21 Bridges between adjacent commercial pages (tienda-online ↔ comercios) + niche links from diseño-web/SEO-local — `docs/seo-audit/full-audit-2026-09-22/tasks/21-puentes-navegacion-paginas-comerciales.md`
- [x] SEO-22 Absolute URL in niche FAQ + llms.txt editorial coverage — `docs/seo-audit/full-audit-2026-09-22/tasks/22-enlace-absoluto-llms-txt.md`
- [x] SEO-23 Question-format H2s + short-answer blocks in 3 blog posts, GEO H2 rewrite, comparison tables in GEO post and `/seo-local-sevilla/` — `docs/seo-audit/full-audit-2026-09-22/tasks/23-estructura-geo-posts-blog.md`
- [ ] SEO-24 through SEO-26 — pending, see `docs/seo-audit/full-audit-2026-09-22/tasks/`

## Progress evidence
(updated per task as completed)

- **SEO-15 (2026-09-22):** Added trailing slash to the 7 shared `href`/`url` values in `src/components/Header.astro` (`navItems` array) and `src/components/Footer.astro` (bottom nav + legal links): `/portfolio/`, `/blog/`, `/sobre-mi/`, `/contacto/`, `/aviso-legal/`, `/privacidad/`, `/cookies/`. Extended `scripts/check-seo-02.mjs` with a `sharedNavPages` list and a per-file `<header>`/`<footer>` bare-href check. RED: extended check failed against unfixed templates (bare hrefs found across all 22 rendered pages). GREEN: `npm run check:seo-02` passed after the fix (`scanned 23 HTML files`). HTTP verification via `BROWSER=none netlify dev --offline --no-open --dir dist --port 8888` + `curl -sSIL --max-redirs 0`: all 7 routes returned `200 OK` direct, no `Location` header. Regression: `check:seo-02` through `check:seo-14` (11 checks) all passed on the same `dist`; `git diff --check` clean. One commit on `develop`.
- **SEO-15 follow-up (2026-09-22):** a `review-reliability` pass on that first commit found a real coverage gap in `check-seo-02.mjs`'s new header/footer scan: `getSections(html, "header")` only captured `<header>...</header>`, but in `Header.astro` the `<header>` closes right after the desktop `<nav>` (~line 128) while the mobile-menu drawer (`<div id="mobile-menu-overlay">` / `<div id="mobile-menu">`, ~lines 130-211) is a sibling `<div>` rendered immediately after, duplicating the same 7 shared-nav links — so a regression limited to the mobile-menu hrefs would pass the check undetected (false negative). RED: temporarily stripped the trailing slash from only the mobile `<a href={link.url}>` for `/portfolio/` (desktop link untouched); `npm run build` + `check:seo-02` still passed despite the bug, confirming the gap; change reverted. Fix: added `getHeaderSections(html)` to `scripts/check-seo-02.mjs`, which locates the adjacent `#mobile-menu` div after `</header>` and includes it via balanced `<div>` counting (not assumed DOM nesting); swapped it in at both call sites that scan the shared header, left the footer scan untouched (no equivalent structure there). GREEN: same RED reproduction now failed correctly (23 issues); reverted, then clean build + `check:seo-02` passed. Regression: `check:seo-02` through `check:seo-14` (11 checks) passed again on the same `dist`; `git diff --check` clean. `Header.astro`/`Footer.astro` untouched (real diff limited to `scripts/check-seo-02.mjs` plus these two doc files). One commit on `develop`, no new task ID.

- **SEO-16 (2026-09-22):** Fixed `src/components/AnalyticsConsent.astro`: `role="dialog"` → `role="region"` (banner is non-blocking, not a real modal — no `aria-modal`/focus trap/Escape-close added, per "No hacer"); 3 action buttons `min-height: 2.75rem` → `3rem` (48px); mobile `max-height: min(34vh, 18rem)` → `min(35vh, 18.5rem)` to remove the ~4px internal overflow at 360px. Also moved `<AnalyticsConsent />` in `src/layouts/Layout.astro` from after `<Footer />` (end of DOM) to immediately after `<body>` opens (before `<Header />`) — this is a DOM-order/focus fix, not a visual change (`position: fixed`), and was needed because Tab order follows DOM order and the panel's buttons were unreachable within 20 Tab presses regardless of ARIA role. RED (Playwright/Python against locally served `dist`): `role="dialog"` no `aria-modal` on all 4 viewports; 360px overflow `scrollHeight 274 vs clientHeight 270` (matches `visual.md`); button heights 45.1875px at 390/414px (matches `visual.md`); 20-Tab reproduction from `visual.md` at 390px never reached the 3 buttons. GREEN: `role="region"`; 360px `scrollHeight 277 == clientHeight 277`; button heights exactly 48px; same 20-Tab reproduction now reaches `#consent-accept`/`#consent-reject`/`#consent-customize` at presses 2/3/4. `Escape` still does not close the panel in either RED or GREEN — correct for `role="region"` (no modal-close semantics to define). Regression: `npm run build` (0 errors) + `check:seo-02` through `check:seo-14` on fresh `dist`; `check:seo-12` initially failed because it hardcoded the old `34vh/18rem`/`2.75rem` values — updated `scripts/check-seo-12.mjs`'s regexes to the new intentional values, then all 11 checks passed. `git diff --check` clean. Verification script was ad-hoc (Python + system Playwright 1.60.0), not added as a permanent project script — existing `check:seo-*` scripts are static-HTML parsers, not browser-interaction tests, so it didn't fit that pattern. One commit on `develop`.

- **SEO-17 (2026-09-22):** Fixed `src/components/WhatsAppButton.astro`. Approach chosen: hide the fixed floating WhatsApp button (via `display:none`, not just opacity) while the home hero's own CTA nav (`[data-seo12-primary-cta]`, which already includes an inline "Cuéntame tu negocio por WhatsApp" CTA) is in the viewport on mobile (`matchMedia("(max-width: 767px)")`), using an `IntersectionObserver` — chosen over widening hero spacing because the floating button is `position:fixed` (its viewport-space position doesn't move with content padding, so a spacing fix couldn't guarantee zero overlap against future hero-copy changes), and because the button is genuinely redundant while the hero's own WhatsApp CTA is visible. No prior scroll/IntersectionObserver logic existed in the codebase to extend, so this introduces the first one, gated defensively (`if (!floatingButton || !heroCta || !("IntersectionObserver" in window)) return`) so it's a no-op on pages without `[data-seo12-primary-cta]` (i.e. every page except the home) and in unsupported browsers (button just stays visible as before, no regression). `display:none` was chosen over `opacity:0` specifically so `getBoundingClientRect()` collapses to a zero-size box (no layout box at all) — an opacity-only hide would still geometrically intersect the CTA per raw DOM rects, even though invisible/non-interactive. Home page and other components (`Hero.astro`, `index.astro`) untouched.
  **RED** (Playwright/Python against pre-fix `dist`, via `git stash push -- src/components/WhatsAppButton.astro`): at 360px, floating button `top:736 left:296 right:344 bottom:784` vs. hero CTA nav `top:680 left:16 right:344 bottom:788` — geometric overlap `True` (confirms the task doc's finding; exact px differ slightly from the original audit due to environment/font rendering, same overlap pattern). 390/414/1440px: no overlap (matches task doc).
  **GREEN** (after fix, same 4 viewports): 360/390/414px — floating button collapses to `top:0 left:0 right:0 bottom:0` (`display:none`) while the hero CTA is in view, overlap `False`; confirmed it reappears (`display:flex`) after scrolling ~1500px past the hero. 1440px — button unaffected (`display:flex`, same position as before), overlap `False` (desktop never triggers the guard since IntersectionObserver only hides under the mobile media query). No horizontal overflow introduced at any of the 4 viewports (`scrollWidth === clientWidth` held in both RED and GREEN). Verified on `/contacto/` and `/sobre-mi/` that the floating button still renders normally (`display:flex`) — it's only suppressed on the home hero, not site-wide. Hero's own inline WhatsApp CTA (`Cuéntame tu negocio por WhatsApp`) unaffected — remains the primary always-visible contact mechanism in that section.
  **Regression:** `npm run build` (0 errors) + `check:seo-02` through `check:seo-14` (11 checks) on the fresh `dist` — all passed, no changes needed to any checker. `git diff --check` clean. Verification script ad-hoc (Python + system Playwright 1.60.0), same pattern as SEO-16, not added as a permanent project script. One commit on `develop`.

- **SEO-17 follow-up (2026-09-22):** a `review-reliability` pass on that first commit found a critical gap: this site has `<ViewTransitions />` enabled site-wide (`src/layouts/Layout.astro`), and Astro does not re-run inline scripts that are byte-identical across pages (as `WhatsAppButton.astro`'s script is, being a shared component) on client-side navigation — only `astro:page-load`/`astro:after-swap` listeners re-run logic per navigation. The SEO-17 script ran once via an IIFE at initial parse and had neither listener, so a user who clicked an internal nav link to the home page (e.g. the header's "Inicio", present on every page) never got the `IntersectionObserver` (re)attached to the freshly-swapped DOM, reproducing the original overlap for what's likely the dominant "return to home" path. Fail-safe direction was already correct (no `display:none` by default, so the guard failing open just meant the button stayed visible, not that it vanished). Fix: extracted the guard into a named function (`initWhatsAppFloatingButtonHeroGuard`) that disconnects any previous `IntersectionObserver` (stored in a module-scoped `whatsAppHeroGuardObserver` variable that survives across un-re-run scripts) before creating a new one, and re-queries the DOM + re-creates the observer on `astro:page-load` — confirmed via `node_modules/astro/dist/transitions/router.js` that this event fires both on the initial hard load (`addEventListener("load", onPageLoad)`) and after every subsequent swap (`onPageLoad()` called after `runScripts()` inside `transition()`), so a single listener now covers both cases (matching `AnalyticsConsent.astro`'s exact pattern). `astro:after-swap` (used by `ThemeToggle.astro`) was considered but rejected: that event exists specifically for paint-critical work that must run before the swapped content is painted (dark-mode class, scroll reset) — this guard isn't paint-critical (the reviewer's fail-safe note already covers a brief visible-button moment), so `page-load`'s slightly later, single-path timing is the better fit and avoids duplicating logic across two lifecycle events.
  **RED** (Playwright/Python against `dist` built from the pre-follow-up-fix source, via `git stash push -- src/components/WhatsAppButton.astro`): loaded `/contacto/` (hard nav), opened the mobile menu, clicked "Inicio" (CSR navigation, not `page.goto`) at 360px viewport — floating button `top:736 left:296 right:344 bottom:784` vs. hero CTA `top:680 left:16 right:344 bottom:788`, geometric overlap `True`, reproducing the original SEO-17 bug via internal navigation even though the hard-load case was already fixed.
  **GREEN** (same reproduction after the fix): floating button collapses to `top:0 left:0 right:0 bottom:0` (`display:none`) after the CSR navigation to home, overlap `False`. Hard-load regression re-verified at 360/390/414/1440px (the original SEO-17 RED/GREEN geometry check): no overlap and no horizontal scroll at any width, 1440px button unaffected — matches the original fix's GREEN evidence, no regression. Repeated-navigation check (`contacto → inicio → contacto → inicio → inicio`, all via CSR nav clicks): button correctly toggles `display:none` on every "inicio" visit (no overlap) and `display:flex` on every "contacto" visit, across 3 round trips — no duplicate/leaked-observer symptoms. (Two unrelated console errors — `Identifier 'ENDPOINT' has already been declared`, from `src/components/ContactForm.astro`'s own inline script on repeated `/contacto/` visits — appeared during this check; confirmed unrelated to the WhatsApp guard and out of scope for SEO-17, left untouched per task constraints. Worth a future backlog item.)
  **Regression:** `npm run build` (0 errors) + `check:seo-02` through `check:seo-14` (11 checks) on the fresh `dist` — all passed, no checker changes needed. `git diff --check` clean. `AnalyticsConsent.astro` and `ThemeToggle.astro` read-only (reference pattern), not modified. One commit on `develop`, no new task ID.

- **SEO-18 (2026-09-22):** Rewrote the closing of the "¿Cómo consigo que aparezca mi negocio...?" FAQ in `src/data/niches.ts` for all 4 niches. The 3 real factors (GBP profile completeness, structured data, recent reviews) were kept verbatim/near-verbatim as the first sentence of each answer (untouched substance, per "No hacer"); only the sentences after that factor sentence were rewritten and trimmed. Each answer went from 4 sentences to 3, ending in a distinct closing/linking sentence with a sector-specific reference to `/seo-local-sevilla/` (fixed the trailing slash, which the old text was missing). Before/after (closing only, factor sentence omitted for brevity):
  - Restaurantes — before: *"No es obligatorio contratarlo para tener una web bien optimizada: es una opción para quien quiere ir un paso más allá."* → after: *"Esa base ya viene incluida en la web que te entrego. Si el mes que viene prefieres no acordarte tú de revisarlo, en /seo-local-sevilla/ cuento cómo llevo ese seguimiento para restaurantes y bares."*
  - Clínicas — before: *"No hace falta contratarlo para tener una web bien planteada: es una opción para quien quiere ir sumando visibilidad mes a mes."* → after: *"La web que te entrego ya nace con esa base montada. Para las clínicas que buscan no depender de acordarse cada mes, en /seo-local-sevilla/ explico el seguimiento de SEO local que hago para consultas."*
  - Comercios — before: *"No es imprescindible contratarlo para tener una web bien optimizada: es una opción para quien quiere seguir ganando visibilidad mes a mes."* → after: *"Ya incluyo esa base en tu web desde el lanzamiento. Quien prefiera no estar pendiente de revisarlo mes a mes encuentra en /seo-local-sevilla/ cómo planteo ese seguimiento para comercios de barrio."*
  - Abogados/gestorías — before: *"No es necesario contratarlo para tener una web bien planteada: es una opción para quien quiere sumar visibilidad de forma continuada."* → after: *"En la web ya queda resuelta esa base desde el primer día. Los despachos que buscan un seguimiento constante, en lugar de revisarlo ellos mismos, encuentran en /seo-local-sevilla/ cómo planteo ese trabajo mensual para despachos y gestorías."*

  The 4 new closings use 4 different grammatical skeletons (subject-first passive "Esa base ya viene incluida...", metaphor lead "La web...ya nace con esa base montada", verb-first first-person "Ya incluyo esa base...", prepositional lead "En la web ya queda resuelta..."), not a shared template with a swapped verb — confirmed by direct side-by-side reading, not just aggregate n-gram similarity. No new numeric claims, case studies, testimonials, or guarantees were added (checked against `.agents/product-marketing-context.md`'s non-negotiable constraints). No em dashes, "cabe destacar", or other flagged AI-writing filler used (no dedicated `ai-writing-detection.md` file was found under the claude-seo plugin skills; used the project's existing em-dash-avoidance convention documented in that plugin's CHANGELOG.md as the closest available reference).
  **Verification:** `npm run build` (0 errors) → `check:seo-05` (`4 rendered niche pages have sector headings, sufficiently distinct copy, no explicit guarded claims, and documented shared blocks`) and `check:seo-07` (`6 intent rows, 32 niche FAQs and 2 reciprocal link pairs checked`) both passed. Full available sweep `check:seo-02` through `check:seo-14` (project has 02, 05-14; no 03/04 exist) — all 11 checks passed on the same `dist`, no checker changes needed. `git diff --check -- src/data/niches.ts` clean. Scope confirmed limited to the 4 FAQ closings in `src/data/niches.ts` plus this doc and the task file — no other file touched.
  One commit on `develop`.

- **SEO-19 (2026-09-22):** Two near-duplicate price FAQ/H2 pairs fixed, both structural/textual differentiation only (no ranking claims — that's SEO-14/GSC).
  1. **Comercios ↔ tienda online (`src/data/niches.ts`):** FAQ `q: "¿Cuánto cuesta una tienda online?"` → `q: "Catálogo o tienda online completa: ¿qué precio tiene cada opción?"`; answer now opens with "Depende de cuál de las dos opciones elijas, porque son dos productos con alcance distinto." before the same unchanged prices (399 € catálogo; tienda online completa sin precio público, presupuesto por alcance). No info removed.
  2. **Diseño web ↔ blog post (`src/data/services.ts`):** H2 `"Cuánto cuesta una página web en Sevilla"` → `"Precio de una página web en Sevilla: desde 149 €"` (transactional, non-interrogative). FAQ `q: "¿Cuánto cuesta una página web en Sevilla?"` → `q: "¿Qué incluye el precio cerrado de una página web en Sevilla?"`, reserving "¿Cuánto cuesta...? Qué influye en el precio" exclusively for `/blog/cuanto-cuesta-una-pagina-web-en-sevilla/` (already documented in `src/data/seo-intents.ts`'s intent matrix). Answer reordered (price + inclusions first, 500-3.000 € market range after) without removing any data.
  3. **Extended `scripts/check-seo-07.mjs`:** added `serviceRoutes` to `src/data/seo-intents.ts` (the 6 service routes) and a niche-vs-service near-duplicate comparison, normalizing away "en Sevilla" and punctuation, flagging only exact matches after normalization (a looser "one contains the other" rule was tried first and rejected — it false-flagged the legitimately distinct restaurantes/diseño-web pair, which merely shares a question prefix). RED: run against the unfixed content, it correctly failed with `niche FAQ near-duplicates a service FAQ: "¿Cuánto cuesta una tienda online?" ~= "¿Cuánto cuesta una tienda online en Sevilla?"`. GREEN: after the rewrite, `npm run check:seo-07` passed (32 niche FAQs checked against 60 service FAQs, 0 errors).
  **Regression:** `npm run build` (0 errors) + `check:seo-02` through `check:seo-14` (11 checks) on the fresh `dist` — all passed, no other checker needed changes (checked `check:seo-05` specifically for niche FAQ regressions, and `check:seo-02`/`check:seo-11` for `/diseno-web-sevilla/` H2/schema structure — none reference the changed H2/FAQ text). `git diff --check` clean. Scope limited to `src/data/niches.ts`, `src/data/services.ts`, `src/data/seo-intents.ts`, `scripts/check-seo-07.mjs` plus these two doc files.
  One commit on `develop`.
  - **Follow-up (2026-09-22), same task ID, found by a `review-reliability` pass on `9b84ee5`:** the niche-vs-service near-duplicate check's exact-equality-after-normalization design (see point 3 above) leaves a real, non-blocking coverage gap — any wording variant beyond `normalizeQuestion()`'s narrow normalization (lowercase, `¿?¡!` stripping, literal `"en sevilla"` removal, whitespace collapse) slips through undetected. Constructed proof: real FAQ `"¿Cuánto cuesta una tienda online en Sevilla?"` (`services.ts:672`) vs. a plausible future niche variant `"¿Cuánto cuesta una tienda online completa?"` — normalized forms differ, so the unfixed checker passes silently despite the real cannibalization risk. Fix: layered a word-trigram Jaccard similarity check (adapted from `check-seo-05.mjs`'s `ngramSimilarity`) on top of (not replacing) the exact-match check, threshold **0.35** — calibrated against the real corpus (highest real-content similarity 0.100; protected restaurantes/diseño-web pair 0.071; the constructed near-duplicate case 0.500). RED (unfixed checker + temporary reproduction FAQ in `niches.ts`, reverted after each run): `SEO-07 intent check passed` (exit 0, gap not caught). GREEN (fixed checker + same reproduction): `SEO-07 intent check failed (1 error) - niche FAQ is a near-duplicate (50.0% word-trigram similarity)...` (exit 1). Reproduction fully reverted before commit (`git diff --check -- src/data/niches.ts` clean). Real content re-verified clean: `npm run check:seo-07` passes (32 niche FAQs vs. 60 service FAQs, 0 errors, including the restaurantes/diseño-web pair). Full regression sweep (`build` + `check:seo-02` through `check:seo-14`, 11 checks) all green. Only permanent change: `scripts/check-seo-07.mjs`. One commit on `develop`.

- **SEO-20 (2026-09-22):** Split dense single-block FAQ context into 2-3 shorter scannable paragraphs, keeping the SEO-08 highlighted lead sentence untouched in function and wording.
  1. **Component (`src/components/services/FAQ.astro`):** the answer text had no paragraph-break support beyond the lead/context split (context rendered as exactly one `<p>`), so a data-only change wasn't possible — enhanced `splitAnswer()` to split the post-lead `context` on `\n\s*\n` into an array of trimmed paragraphs (falls back to `[]`/no `<p>` when empty, same as before) and the template now maps that array to one `<p>` per paragraph. FAQs without a `\n\n` in their data render exactly as before (0 visual change) — verified against the 78 untouched FAQs via `check:seo-08`.
  2. **Data — 20 FAQs restructured** by inserting `\n\n` between existing sentences (no rewording, no new claims/numbers, real prices untouched): all price/plazo FAQs across the 4 niche pages and the 6 service pages (18 total: restaurantes/clínicas/comercios/abogados price; diseño-web price+plazo+399€-incluye; software price+plazo; automatizaciones price+plazo; IA price+plazo; SEO local price+proyecto-inicial+plazo; tienda-online price+plazo), plus the 2 explicitly confirmed non-transactional examples from the task's Problema section: restaurantes "¿Me ayudas también con las reseñas de Google?" and clínicas "¿Cómo tratáis los datos de los pacientes?" (RGPD — split only at existing sentence boundaries, zero legal nuance removed, verified by side-by-side reading).
  3. **Skipped (lower priority, left for a future pass, not touched):** all other niche/service FAQs (~58 remaining, incl. abogados "¿Hay límites para la publicidad...?" deontology FAQ and the other "cómo aparezco cerca de mí" FAQ closings already rewritten by SEO-18) — confirmed via a word-count script that they still sit at 70-130 words in a single block. None were force-split to hit a line-count target; scope was intentionally bounded to transactional FAQs plus the 2 confirmed examples per the task's explicit prioritization and the "don't rush all of them" allowance.
  4. **Word-count verification (script-based, not eyeballed):** ran a Node/tsx script against the built data importing `splitAnswer`'s exact logic on all 20 restructured FAQs — all context paragraphs land at ≤62 words (most 20-60), down from single blocks of 73-160 words. Two FAQs (clínicas price, comercios price) needed a second pass — the first split left one paragraph at 73-100 words — fixed by adding one more `\n\n`.
  **Regression:** `npm run build` (0 errors) → `check:seo-08` (`98 FAQs and GEO article structure validated`, same FAQ count as before — no FAQs added/removed, only restructured) → full sweep `check:seo-02` through `check:seo-14` (11 checks) on the fresh `dist`, all passed, including `check:seo-05` and `check:seo-07` (niche/service content and near-duplicate FAQ-question checks, unaffected since only `a` answer bodies changed, not `q` questions). `git diff --check` clean (no trailing whitespace from the `\n\n` inserts). Manually inspected rendered HTML for 2 FAQs (`tienda-online-sevilla`, `web-para-comercios-sevilla`) to confirm multiple `<p>` tags render correctly inside `<details>`.
  Scope limited to `src/data/niches.ts`, `src/data/services.ts`, `src/components/services/FAQ.astro` plus these two doc files.
  One commit on `develop`.
  - **Follow-up (2026-09-22), same task ID, found by a `review-reliability` pass on `cdb4726`:** `faqSchema()` in `src/lib/schema.ts` built `FAQPage`'s `acceptedAnswer.text` from the raw `faq.a` string, not through `splitAnswer()`. For the 20 FAQs restructured above with embedded `\n\n`, that literal double-newline leaked into the structured-data `text` field (JSON-escaped `\n\n`, no whitespace normalization in `Schema.astro`'s `JSON.stringify`) — a real content regression in what search engines/AI see as the answer text. No `check-seo-*.mjs` validates `acceptedAnswer.text` content, so nothing caught it. Fix: `faqSchema()` now normalizes with `faq.a.replace(/\s+/g, " ").trim()` before assigning to `acceptedAnswer.text`, collapsing `\n\n` and any other internal whitespace into single-spaced prose; `FAQ.astro`/`splitAnswer()` untouched (still renders the visual paragraph breaks). RED: build + inspected the generated `web-para-restaurantes-sevilla` page's `FAQPage` JSON-LD for the restaurantes pricing FAQ (`niches.ts:72`) — confirmed literal `\n\n` between its 3 paragraphs in `acceptedAnswer.text`. GREEN: same build/inspection after the fix — clean continuous prose, no `\n`, no double spaces; a sampled unrestructured FAQ ("¿Puedo actualizar la carta yo mismo?") is byte-for-byte identical to its pre-`cdb4726` schema output. Regression: `npm run build` + full `check:seo-02` through `check:seo-14` sweep (11 checks) on fresh `dist`, all passed, including `check:seo-11` (schema validation). `git diff --check` clean. Only permanent change: `src/lib/schema.ts` plus these two doc files. One commit on `develop`.

- **SEO-21 (2026-09-22):** Added explicit in-content navigation bridges between adjacent commercial pages, plus niche-page links from the two hub service pages, without merging any page or changing its documented intent in `src/data/seo-intents.ts`.
  1. **Bidirectional bridge, tienda-online ↔ comercios (visible copy, not just `internal-links.ts`):**
     - `src/pages/tienda-online-sevilla.astro` (existing top `before-benefits` intro block, 4th paragraph added): *"Si lo único que necesitas es mostrar tu catálogo y tus horarios, sin cobrar online todavía, seguramente te encaja mejor la [web para comercios](/web-para-comercios-sevilla/)."*
     - `src/pages/web-para-comercios-sevilla.astro` (previously had zero page-specific content — added a new `before-benefits` block, matching the site's existing intro-block convention): title *"¿Solo necesitas catálogo, o también quieres cobrar online?"*, then *"Esta página cubre el catálogo: que tus clientes vean qué vendes, tus horarios y cómo contactarte por WhatsApp o pasando por tienda."* followed by *"Si además quieres cobrar online, con pasarela de pago y gestión de stock, mira la [tienda online en Sevilla](/tienda-online-sevilla/)."*
     - Enforced by a new `reciprocalLinks` entry in `src/data/seo-intents.ts` (source `/tienda-online-sevilla/` → target `/web-para-comercios-sevilla/`, anchors `"web para comercios"` / `"tienda online en Sevilla"`), checked by `check:seo-07` (raw HTML `href` + anchor-text presence both directions) — same mechanism as the 2 pre-existing informational→commercial pairs, now covering a commercial↔commercial pair for the first time.
  2. **`/diseno-web-sevilla/` — niche link moved higher:** added a 4-pill niche-page list (reused the exact pattern/markup already used on the homepage, `src/pages/index.astro`) right inside the existing top `before-benefits` block, above Benefits/Process/Pricing and the bottom "Contenido relacionado" block: *"¿Tienes un restaurante, una clínica, un comercio o un despacho? Mira la página dedicada a tu sector:"* + links to all 4 niche pages (`niches` imported from `src/data/niches.ts`).
  3. **`src/data/internal-links.ts` — new entries (bottom "Contenido relacionado" block, rendered by the existing `InternalLinks` component):**
     - `/diseno-web-sevilla/` gained 4 entries: `/web-para-restaurantes-sevilla/` ("web para restaurantes en Sevilla"), `/web-para-clinicas-sevilla/` ("web para clínicas y consultas"), `/web-para-comercios-sevilla/` ("web para comercios y tiendas"), `/web-para-abogados-gestorias-sevilla/` ("web para despachos y gestorías").
     - `/seo-local-sevilla/` gained the same 4 niche routes with SEO-local-flavored anchors: "SEO local para restaurantes en Sevilla", "SEO local para clínicas en Sevilla", "SEO local para comercios en Sevilla", "SEO local para despachos y gestorías".
  **No pages merged, no `intentMatrix` rows changed** — only additive `reciprocalLinks`/`internal-links.ts` entries and in-content copy. No search-experience "resolved" claim made (SEO-14/GSC still pending, per task's "No hacer").
  **Verification:** `npm run build` (0 errors) → `check:seo-06` (`15 mapped sources and 23 rendered HTML files checked`, was 15/23 before too — no source added/removed, only entries within existing sources) → `check:seo-07` (`6 intent rows, 32 niche FAQs..., 3 reciprocal link pairs checked`, up from 2) — both passed. Full sweep `check:seo-02` through `check:seo-14` (11 checks total) on the same fresh `dist` — all passed, no checker changed. `git diff --check` clean. Manually verified via `rg` on `dist/**/*.html`: `href="/web-para-comercios-sevilla/"` + `web para comercios` present on the rendered tienda-online page; `href="/tienda-online-sevilla/"` + `tienda online en Sevilla` present on the rendered comercios page; all 4 niche `href`s present on the rendered diseño-web page — all with the site's trailing-slash convention, all real published routes (confirmed against `src/pages/*.astro`).
  Scope limited to `src/data/internal-links.ts`, `src/data/seo-intents.ts`, `src/pages/tienda-online-sevilla.astro`, `src/pages/web-para-comercios-sevilla.astro`, `src/pages/diseno-web-sevilla.astro`, plus these two doc files.
  One commit on `develop`.

- **SEO-21 follow-up (2026-09-22):** a `review-reliability` pass on commit `e3c029c` found a real CRITICAL content contradiction: the new `before-benefits` bridge paragraph on `/web-para-comercios-sevilla/` claimed "esta página cubre el catálogo" and pointed to `/tienda-online-sevilla/` for anyone wanting "cobrar online, con pasarela de pago y gestión de stock" — but that same page's own pre-existing content (`src/data/niches.ts`, `comercios` object: benefit "Venta online sin comisión de marketplace" offering `tienda online propia (WooCommerce o similar)` with Stripe/Redsys, the pricing note "la tienda online completa con pago y envíos se presupuesta...", and both FAQs contrasting catálogo vs. tienda online completa) already sells a full online store with payment as an option of this same service. A visitor read "catalog-only" at the top and saw the page sell online payment a few sections later.
  Confirmed the real differentiation by reading `src/data/services.ts`'s `tiendaOnline` object: `/web-para-comercios-sevilla/` is not catalog-only — `/tienda-online-sevilla/` is the dedicated page that goes deeper specifically on the e-commerce side (detailed payment-gateway integration and testing, stock/variant management, digital products, shipping, Shopify/Wix comparison, IVA/invoicing legal aspects), while the comercios page offers the online-store option without that depth.
  **Fix** — `src/pages/web-para-comercios-sevilla.astro`, same `before-benefits` block, title unchanged:
  - Before: *"Esta página cubre el catálogo: que tus clientes vean qué vendes, tus horarios y cómo contactarte por WhatsApp o pasando por tienda."* + *"Si además quieres cobrar online, con pasarela de pago y gestión de stock, mira la tienda online en Sevilla."*
  - After: *"Esta página cubre las dos opciones para tu comercio: catálogo para que tus clientes vean qué vendes, tus horarios y cómo contactarte por WhatsApp o pasando por tienda, y tienda online completa con pasarela de pago si ya quieres vender sin salir de la web."* + *"Si tu prioridad es la venta online -- pasarela de pago, gestión de stock, productos digitales, envíos -- entra en la tienda online en Sevilla, la página donde profundizo en todo eso."*
  Reverse bridge on `src/pages/tienda-online-sevilla.astro` ("Si lo único que necesitas es mostrar tu catálogo y tus horarios, sin cobrar online todavía, seguramente te encaja mejor la web para comercios") independently checked against that page's own benefits/pricing/FAQ content — no contradiction found, left unchanged.
  **Regression:** `npm run build` (0 errors) + `check:seo-06` and `check:seo-07` green + full `check:seo-02`–`check:seo-14` sweep (11 checks) green on fresh `dist` + `git diff --check` clean. Only `src/pages/web-para-comercios-sevilla.astro` changed; `niches.ts`/`services.ts` untouched. One commit on `develop`, no new task ID (same SEO-21).

- **SEO-22 (2026-09-22):** **Task-doc discrepancy found before implementing.** The task doc quotes the "problema" text as *"...tengo un servicio aparte de SEO local que puedes consultar en **/seo-local-sevilla**."* — that text no longer exists. SEO-18, earlier this same session, already rewrote the closing sentence of the same 4 niche FAQs and changed the wording entirely. Re-read `src/data/niches.ts` fresh instead of trusting the quote: the current text (e.g. restaurantes) reads *"...en /seo-local-sevilla/ cuento cómo llevo ese seguimiento para restaurantes y bares."* — SEO-18 did add the trailing slash, but the citability gap described in `geo.md` was still present in a new form: the visible FAQ answer text (rendered via `src/components/services/FAQ.astro`'s `{paragraph}` string interpolation, confirmed by reading the component — plain text, no markdown/link parsing) still exposed a bare relative path (`/seo-local-sevilla/`) with no domain, not resolvable as a citation outside the site. Also confirmed the task doc's premise of "the `href` is correct, don't touch it" doesn't apply here: this specific mention isn't a link at all (no `<a href>`) — it's plain prose text, so there was no navigational `href` to preserve for this instance (the real navigational `href`s for that route live in `src/data/internal-links.ts`'s "Contenido relacionado" blocks and were untouched).
  **Fix:** replaced the bare `/seo-local-sevilla/` with `https://nuriaromero.dev/seo-local-sevilla/` in the visible answer text of all 4 niche FAQs in `src/data/niches.ts` (restaurantes, clínicas, comercios, abogados/gestorías) — wording otherwise unchanged from SEO-18's version, no new claims or numbers added.
  - Before (all 4): *"...en /seo-local-sevilla/ [cuento/explico/encuentra/encuentran]..."*
  - After (all 4): *"...en https://nuriaromero.dev/seo-local-sevilla/ [cuento/explico/encuentra/encuentran]..."*
  **llms.txt:** `public/llms.txt`'s "Contenido editorial" section only individually linked 1 of 4 blog posts. Read each post's frontmatter (`src/content/blog/*.md`) for accurate one-sentence descriptions (no invented claims) and added the other 3, same format (absolute URL + one-sentence description), ordered chronologically by `pubDate` before the existing GEO entry: `pagina-web-para-restaurantes-que-necesita` (2026-06-24), `como-aparecer-en-google-maps-negocio-sevilla` (2026-07-01), `cuanto-cuesta-una-pagina-web-en-sevilla` (2026-07-07).
  **Verification:** `npm run build` (0 errors) → `check:seo-09` (`22 llms.txt links validated`, was fewer before the 3 additions) passed. Full sweep `check:seo-02` through `check:seo-14` (11 checks; project has no 03/04) on the fresh `dist` — all passed, no checker changes needed. `git diff --check` clean. Read the rendered `dist/llms.txt` directly — all 4 posts now individually linked with absolute URLs. Read the 4 rendered niche pages' HTML (`dist/web-para-*-sevilla/index.html`) via `rg -o "https://nuriaromero\.dev/seo-local-sevilla/[^<\"]*"` — confirmed the absolute URL is present in the visible FAQ text (and duplicated correctly in the `FAQPage` JSON-LD, which mirrors the same string per `src/lib/schema.ts`'s `faqSchema()`).
  Scope limited to `src/data/niches.ts`, `public/llms.txt`, plus these two doc files. Unrelated untracked files (`research/`, `.seo-cache/`) left untouched.
  One commit on `develop`.

- **SEO-23 (2026-09-22):** Rewrote H2s to explicit interrogative format (`¿...?`) in the GEO post (`que-es-geo-posicionamiento-ia-negocios-sevilla.md`) — e.g. "Qué es el SEO local" → "¿Qué es el SEO local?", "Cuatro cosas que puedes hacer ya" → "¿Qué cuatro cosas puedes hacer ya?" (its existing "Respuesta corta" H2 was left untouched — it's a label, not a question, and `check:seo-08` asserts that exact heading pattern). Added a "Respuesta corta" block (1-2 self-contained sentences, no new claims) plus question-format H2s to the 3 other posts, replicating the GEO post's pattern: `como-aparecer-en-google-maps-negocio-sevilla.md` ("Paso 1: crea o reclama tu ficha" → "¿Cómo creo o reclamo mi ficha de Google Business Profile?", 5 steps total), `cuanto-cuesta-una-pagina-web-en-sevilla.md` ("Las 5 preguntas que debes hacer antes de contratar" → "¿Qué preguntas debo hacer antes de contratar una web?"), `pagina-web-para-restaurantes-que-necesita.md` ("Lo imprescindible" → "¿Qué es imprescindible en la web de un restaurante?"). All existing advice/facts/links preserved verbatim — only heading framing and the new short-answer paragraphs changed; H3 sub-steps in the restaurant post left untouched (task scope was H2s).
  Added one markdown comparison table to the GEO post ("¿En qué se diferencian el SEO local y el GEO?": what each optimizes for, where it shows up, what base it needs, what neither guarantees — conceptual, no figures) and one HTML table to `/seo-local-sevilla/` (`src/pages/seo-local-sevilla.astro`, new `SectionContainer` placed right after the pricing cards) comparing the Básico/Competitivo tiers: 199 €/mes vs. 349 €/mes, 1 pieza vs. 2-3 piezas de contenido, recommended-for text, 3-month minimum commitment — every figure sourced verbatim from `src/data/services.ts`'s existing `seoLocal.pricing.tiers` (also cross-checked against `.agents/product-marketing-context.md`), no numbers invented.
  **Side fix required to stay green:** `check:seo-07`'s `src/data/seo-intents.ts` `contentSignals` hardcoded the old (now-renamed) H2 text for the GEO and pricing posts as distinctiveness signals; updated those 4 strings to the new question-format headings (e.g. `"Cuatro cosas que puedes hacer ya"` → `"Qué cuatro cosas puedes hacer ya"`). No other data files needed changes.
  **Verification:** `npm run build` (0 errors) → full `check:seo-02` through `check:seo-14` sweep (11 checks) on fresh `dist` — all passed (initial run surfaced the `check:seo-07` signal-string mismatch above; fixed, rebuilt, reran — all green, including `check:seo-08`'s GEO-specific "Respuesta corta" heading assertion). `git diff --check` clean. Manual re-read of every new/changed H2, respuesta-corta paragraph, and table cell against `.agents/product-marketing-context.md`: no invented figures, timeframes, guarantees, or ranking claims found; both tables state explicitly that no tier/discipline guarantees a Google position. Video/infographic production untouched (out of scope per task doc). Scope limited to the 3 blog posts, the GEO post, `seo-local-sevilla.astro`, `seo-intents.ts`, plus these two doc files; unrelated untracked files (`research/`, `.seo-cache/`) left untouched.
  One commit on `develop`.

## Next step
SEO-24 through SEO-26 pending, one at a time per user instruction — see `docs/seo-audit/full-audit-2026-09-22/tasks/`.
